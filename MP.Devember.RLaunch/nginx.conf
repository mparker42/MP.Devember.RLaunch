events { }
http {
    server {
        listen 80;
        listen [::]:80;

        location / {
            root /usr/share/nginx/html;
            try_files $uri $uri/ /index.html =404;

            include mime.types;
            default_type application/octet-stream;

            location /_framework/ {
                gzip_static on;
            }
        }
    }
}